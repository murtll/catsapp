/*
  Config for MinIO S3
  TODO change this before start!
*/

export const config = {
  endPoint: 'host',
  port: 9000,
  useSSL: false,
  accessKey: 'minioadmin',
  secretKey: 'minioadmin',
  baseBucket: 'cats-test',
}
