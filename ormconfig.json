{
    "name": "default",
    "type": "postgres",
    "url": "postgres://user:password@host:PORT/database",
    "entities": ["dist/**/*.entity{.ts,.js}"],
    "synchronize": true,
    "ssl": true,
    "extra": {
        "ssl": {
            "rejectUnauthorized": false
        }
    }
}